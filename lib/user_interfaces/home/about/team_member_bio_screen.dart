import 'package:flutter/material.dart';

class TeamMemberBioScreen extends StatelessWidget {
  static String routeName = 'teamMemberBio';

  const TeamMemberBioScreen({super.key});
  @override
  Widget build(BuildContext context) {
    return Scaffold();
  }
}
